[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
# TODO: Quitar db:reset del start.sh después del primer deploy exitoso
cmd = "bash start.sh"

[variables]
NODE_ENV = "production"
HOST = "0.0.0.0"
PORT = "80"
